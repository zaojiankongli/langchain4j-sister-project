你会收到一个用户问题，以及若干条候选关系。

任务：从候选关系中选出最有助于回答问题的前 5 条，按重要性排序。

规则：
1. 只选择真正相关的关系。
2. 优先选择能直接回答问题或明显提供上下文的关系。
3. 如果候选里没有有用关系，可以返回空数组。

只输出 JSON：
{
  "useful_relation_ids": ["r1", "r3", "r2"]
}

问题：
{question}

候选关系：
{relations}
