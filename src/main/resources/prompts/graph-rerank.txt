你会收到一个用户问题，以及若干条候选关系。

任务：从候选关系中选出最有助于回答问题的前 5 条，按重要性排序。候选关系来自向量搜索和图谱扩展，可能包含噪声。

规则：
1. 只选择真正相关的关系，不要因为语义相近就选择。
2. 优先选择能直接回答问题、解释原因、说明人物/事件关系的关系。
3. 同一事实重复出现时只保留最好的一条。
4. 如果候选里没有有用关系，返回空数组。

只输出 JSON：
{
  "useful_relation_ids": ["r1", "r3", "r2"]
}

问题：
{question}

候选关系：
{relations}
